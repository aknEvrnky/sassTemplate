// bootstrap the libraries

window.$ = require('jquery');

import {Button, Modal, Offcanvas} from 'bootstrap'
const bootstrap = bootstrap;

window.fontawesome = require('@fortawesome/fontawesome-pro/js/all.min')
